name: Test
run-name: "Test: ${{ github.event.workflow_run.jobs.tag.outputs.tag }}"
on:
  push:
    branches: [ "main" ]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: '[[ "$(cat test)" == "pass" ]]'